// lib/Screens/LearnerScreen/learner_profile_page.dart

import 'package:flutter/material.dart';

class LearnerProfilePage extends StatelessWidget {
  const LearnerProfilePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // Replace with your actual Profile layout
    return const Center(
      child: Text(
        'Profile Page',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
